// convert mile to km
final float MILES_KM_CONVERSION_VALUE = 1.61;
float homeToSchool = 30 * MILES_KM_CONVERSION_VALUE;

println("30 miles = " + homeToSchool + "km");

// calculate the area of a circle
final float PI = 3.14;
float circleArea;
float circleRadius = 5; // unit: cm

circleArea = circleRadius * circleRadius * PI;

println("circle area = " + circleArea);